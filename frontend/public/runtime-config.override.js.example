/**
 * ARCHIVO DE EJEMPLO - Configuración de sobrescritura para el servidor
 * 
 * Para usar este archivo en producción:
 * 1. Copiar este archivo como 'runtime-config.override.js' en el servidor
 * 2. Modificar las URLs según tu configuración específica
 * 3. Este archivo NO debe ser commiteado a GitLab
 * 
 * En el servidor VPS:
 * cp /var/www/9001app2/dist/runtime-config.override.js.example /var/www/9001app2/dist/runtime-config.override.js
 * 
 * Luego editar el archivo con las URLs correctas
 */

// Sobrescribir configuración específica del servidor
window.__RUNTIME_CONFIG__ = window.__RUNTIME_CONFIG__ || {};

// Ejemplo de configuración para servidor específico
// Descomenta y modifica según necesites:

/*
window.__RUNTIME_CONFIG__ = {
  ...window.__RUNTIME_CONFIG__,
  API_BASE_URL: 'http://31.97.162.229:5000/api',
  API_URL: 'http://31.97.162.229:5000/api',
  // Puedes agregar más configuraciones específicas aquí
};
*/

// Para un servidor con dominio:
/*
window.__RUNTIME_CONFIG__ = {
  ...window.__RUNTIME_CONFIG__,
  API_BASE_URL: 'https://api.tudominio.com/api',
  API_URL: 'https://api.tudominio.com/api',
};
*/

// Para un servidor con proxy reverso (nginx):
/*
window.__RUNTIME_CONFIG__ = {
  ...window.__RUNTIME_CONFIG__,
  API_BASE_URL: '/api',  // Si nginx hace proxy del backend en la misma URL
  API_URL: '/api',
};
*/
